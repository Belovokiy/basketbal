#include <string>


class UserUnterface {
public:
	void commandControl(std::string commandName);
	void viewGame(std::string commandName);
	void originalProductionPublishing(std::string commandName);
	void gameOrganization(std::string commandName);
};